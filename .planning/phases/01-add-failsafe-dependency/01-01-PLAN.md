---
phase: 01-add-failsafe-dependency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
autonomous: true
requirements:
  - DEP-01
must_haves:
  truths:
    - "mvn compile succeeds with dev.failsafe:failsafe:3.3.2 resolvable on the classpath"
    - "No source file under src/ references dev.failsafe (failsafe present but unused)"
    - "All existing tests pass unchanged (guava-retrying still the active engine)"
  artifacts:
    - path: "pom.xml"
      provides: "failsafe.version property + compile-scope failsafe dependency adjacent to guava-retrying"
      contains:
        - "<failsafe.version>3.3.2</failsafe.version>"
        - "<groupId>dev.failsafe</groupId>"
        - "<artifactId>failsafe</artifactId>"
        - "<version>${failsafe.version}</version>"
  key_links:
    - from: "<failsafe.version> property in <properties>"
      to: "<version>${failsafe.version}</version> in failsafe dependency block"
      via: "Maven property interpolation"
      pattern: "\\$\\{failsafe\\.version\\}"
---

<objective>
Add `dev.failsafe:failsafe:3.3.2` to the Maven classpath without touching any source. The tree still compiles and runs on guava-retrying; nothing uses failsafe yet. This phase delivers stack readiness only.

Purpose: Landing failsafe first (while keeping guava-retrying) lets the tree compile at every subsequent step. Removing guava-retrying first would break compilation until every impl is ported in Phase 2.

Output: `pom.xml` with a `<failsafe.version>` property and a compile-scope `<dependency>` block for failsafe, adjacent to the existing guava-retrying block.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-add-failsafe-dependency/01-CONTEXT.md
@.planning/phases/01-add-failsafe-dependency/01-RESEARCH.md
@pom.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add failsafe dependency property and block to pom.xml</name>
  <files>pom.xml</files>
  <read_first>
    - pom.xml (read the full file; specifically <properties> lines 96-108 and the guava-retrying <dependency> block lines 178-182 — these are the edit anchors and the convention to replicate)
    - .planning/phases/01-add-failsafe-dependency/01-CONTEXT.md (locked decisions D-01 through D-04)
    - .planning/phases/01-add-failsafe-dependency/01-RESEARCH.md (verified coordinates, zero-transitive-dep confirmation, anti-patterns)
  </read_first>
  <action>
Make two edits to pom.xml. Both follow the project's existing conventions exactly (every dep has a `*.version` property; compile-scope deps omit the `<scope>` tag).

EDIT 1 — Add the version property (per D-01, D-04):
In the `<properties>` block, immediately AFTER the existing line:
    <guava-retrying.version>2.0.0</guava-retrying.version>
(line 107), insert:
    <failsafe.version>3.3.2</failsafe.version>
This groups the two retry-library version properties together, so Phase 3's removal of `guava-retrying.version` is a clean adjacent edit.

EDIT 2 — Add the dependency block (per D-02, D-03):
In the `<dependencies>` block, immediately AFTER the closing `</dependency>` of the guava-retrying block (line 182), insert:
    <dependency>
        <groupId>dev.failsafe</groupId>
        <artifactId>failsafe</artifactId>
        <version>${failsafe.version}</version>
    </dependency>
Do NOT add a `<scope>` tag — compile is the default and the project convention (guava-retrying, commons-lang3, httpclient all omit it). Do NOT inline `<version>3.3.2</version>` — use `${failsafe.version}` per D-01. Do NOT add an XML comment — keep the block minimal and matching the guava-retrying block's shape (no comment above it).

Use the exact groupId `dev.failsafe` (NOT the deprecated `net.jodah` — see RESEARCH.md Pitfall 1). Use the exact version `3.3.2` in the property (NOT a range or BOM-managed version, per D-04).

Do NOT touch any `.java` file. Do NOT add any `import dev.failsafe.*` anywhere. Phase 1 is pom-only — the exit gate forbids any `dev.failsafe` reference in `src/`.
  </action>
  <verify>
    <automated>cd /home/phaneesh/devzone/oss/dropwizard-rabbitmq-actors &amp;&amp; grep -c '<failsafe.version>3.3.2</failsafe.version>' pom.xml | grep -q 1 &amp;&amp; grep -A4 '<groupId>dev.failsafe</groupId>' pom.xml | grep -q '<version>${failsafe.version}</version>' &amp;&amp; ! grep -rn 'dev.failsafe' src/ &amp;&amp; mvn -q compile &amp;&amp; mvn -q test</automated>
  </verify>
  <done>
    - pom.xml contains `<failsafe.version>3.3.2</failsafe.version>` in `<properties>`, immediately after `guava-retrying.version`
    - pom.xml contains a `<dependency>` block with groupId `dev.failsafe`, artifactId `failsafe`, version `${failsafe.version}`, no`<scope>` tag, immediately after the guava-retrying block
    - `mvn compile` exits 0 (failsafe 3.3.2 resolves from Maven Central)
    - `mvn test` exits 0 (all existing tests pass unchanged on guava-retrying)
    - `grep -rn 'dev.failsafe' src/` returns no matches (no source references failsafe)
  </done>
  <acceptance_criteria>
    - `grep -c '<failsafe.version>3.3.2</failsafe.version>' pom.xml` outputs `1`
- `grep -A4 '<groupId>dev.failsafe</groupId>' pom.xml` contains `<version>${failsafe.version}</version>`
- `grep -A5 '<groupId>dev.failsafe</groupId>' pom.xml` does NOT contain `<scope>` (compile scope by omission)
    - `grep -rn 'dev.failsafe' src/` exits non-zero (no matches)
    - `mvn compile` exits 0
    - `mvn test` exits 0
    - `grep -c 'net.jodah' pom.xml` outputs `0` (deprecated groupId not used)
  </acceptance_criteria>
</task>

</tasks>

<verification>
Phase exit gate (all must hold):
1. `mvn compile` succeeds with `dev.failsafe:failsafe:3.3.2` resolvable on the classpath.
2. `grep -rn 'dev.failsafe' src/` returns no matches — failsafe is present but unused.
3. `mvn test` passes — all existing tests green, guava-retrying still the active engine.
4. `grep -c '<failsafe.version>3.3.2</failsafe.version>' pom.xml` outputs `1`.
5. `grep -A5 '<groupId>dev.failsafe</groupId>' pom.xml` contains `<version>${failsafe.version}</version>` and no `<scope>` tag.
</verification>

<success_criteria>

- failsafe 3.3.2 is on the classpath (mvn compile resolves it).
- Zero source files reference dev.failsafe (grep gate clean).
- All existing tests pass unchanged (mvn test green).
- pom.xml follows project conventions: version property + ${} reference, compile scope by omission, adjacent to guava-retrying.
</success_criteria>

<output>
After completion, create `.planning/phases/01-add-failsafe-dependency/01-01-SUMMARY.md`
</output>
