---
phase: 04-release-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MIGRATION.md
  - CHANGELOG.md
  - README.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
must_haves:
  truths:
    - "Changelog entry names RetryException → FailsafeException/raw-throwable change and flags it as a transitive compile break"
    - "Migration note states execute() retains throws Exception declaration and synchronous blocking is unchanged"
    - "Both docs reference failsafe 3.3.2 and removed guava-retrying coordinate (com.github.rholder:guava-retrying:2.0.0)"
    - "Changelog links to MIGRATION.md"
    - "Docs are consistent with verified Phase 3 behavior (49 tests green, guava fully removed)"
  artifacts:
    - path: "MIGRATION.md"
      provides: "Migration note for retry-engine swap"
      contains: "throws Exception"
    - path: "CHANGELOG.md"
      provides: "Changelog entry for 5.0.2-1"
      contains: "FailsafeException"
  key_links:
    - from: "CHANGELOG.md"
      to: "MIGRATION.md"
      via: "markdown link"
      pattern: "MIGRATION\\.md"
---

<objective>
Write release documentation (changelog entry + migration note) for the guava-retrying → failsafe retry-engine swap. This is the final phase of the 4-phase migration. No source code changes — documentation only.

Purpose: Downstream consumers catching `RetryException` by type will hit a compile break. This must be documented, not discovered at compile time.

Output: New `MIGRATION.md` at repo root, new top section in `CHANGELOG.md`, optional one-line pointer in `README.md`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-release-documentation/04-CONTEXT.md
@.planning/phases/03-remove-guava-retrying-verify-parity/03-VERIFICATION.md

<interfaces>
<!-- Verified facts from Phase 3 that the docs must be consistent with -->

RetryStrategy.java (the public API being documented):

```java
public abstract class RetryStrategy {
    private final RetryPolicy<Boolean> retryPolicy;

    public boolean execute(Callable<Boolean> callable) throws Exception {
        return Failsafe.with(retryPolicy).get(() -> callable.call());
    }
}
```

Key facts:

- execute() signature: `public boolean execute(Callable<Boolean> callable) throws Exception` — UNCHANGED
- Failsafe.get() throws FailsafeException (unchecked, extends RuntimeException) wrapping the last failure, OR the raw throwable if it's an Error
- guava's RetryException was checked — consumers catching it by type get a compile error (type no longer on classpath)
- Handler.handleDelivery() catches Throwable — internal call site unaffected
- pom.xml: dev.failsafe:failsafe:3.3.2 present, com.github.rholder:guava-retrying removed
- Phase 3: 49 tests green on failsafe-only tree, 15 parity tests prove behavioral equivalence
- Current version in pom.xml: 5.0.2-1
- CHANGELOG.md style: loose bullets, `## <version>` headings, newest-first, no Added/Changed/Removed sub-grouping
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Write MIGRATION.md</name>
  <files>MIGRATION.md</files>
  <action>
Create a new `MIGRATION.md` at repo root. Concise, factual tone (per D-07). Sections:

**## Retry Engine Migration: guava-retrying → failsafe**

Short intro: This release replaces the unmaintained `com.github.rholder:guava-retrying:2.0.0` with `dev.failsafe:failsafe:3.3.2` as the retry engine. All retry semantics (attempt counts, wait sequences, exception filtering, synchronous blocking) are behaviorally identical — verified by 15 parity tests.

**### What changed**

- The exhaustion exception type changed from guava's checked `com.github.rholder.guava.retrying.RetryException` to failsafe's unchecked `dev.failsafe.FailsafeException` (which extends `RuntimeException`), or the raw original throwable if it's an `Error`.
- `RetryException` is no longer on the classpath — `catch (RetryException e)` will not compile.

**### What did NOT change**

- `RetryStrategy.execute(Callable<Boolean>)` retains its `throws Exception` declaration (per D-04). The signature is byte-identical to the pre-migration API.
- Synchronous blocking behavior is unchanged — `Failsafe.with(policy).get()` blocks the calling thread, same as guava's `Retryer.call()`.
- All retry semantics (attempt counts, sleep sequences, exception filtering) are behaviorally identical, proven by 15 parity tests on the failsafe-only tree (49 total tests green).

**### What to do**

If your code catches `RetryException` by type:

```java
// Before (broken — RetryException no longer exists)
try {
    strategy.execute(callable);
} catch (RetryException e) {
    // handle exhaustion
}

// After — execute() already declares throws Exception
try {
    strategy.execute(callable);
} catch (Exception e) {
    // handle exhaustion (FailsafeException wraps the last failure)
}
```

If you already catch `Exception` or `Throwable` around `execute()`, no change needed — your existing handler covers `FailsafeException`.

**### Version references**

- Removed: `com.github.rholder:guava-retrying:2.0.0`
- Added: `dev.failsafe:failsafe:3.3.2`

Include a one-line note that `Handler.handleDelivery()` (the internal call site) catches `Throwable`, so internal behavior is unaffected — only consumer-side typed catches break.
  </action>
  <verify>
    <automated>test -f MIGRATION.md &amp;&amp; grep -q "throws Exception" MIGRATION.md &amp;&amp; grep -q "3.3.2" MIGRATION.md &amp;&amp; grep -q "2.0.0" MIGRATION.md &amp;&amp; grep -q "FailsafeException" MIGRATION.md &amp;&amp; echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>MIGRATION.md exists at repo root with: exception-type change documented, throws Exception contract stated, synchronous blocking unchanged stated, version references for both libs, before/after code snippet for catch change.</done>
</task>

<task type="auto">
  <name>Task 2: Write changelog entry and README pointer</name>
  <files>CHANGELOG.md, README.md</files>
  <action>
**CHANGELOG.md** (per D-01, D-02, D-03, D-06, D-07):

Add a new `## 5.0.2-1` section at the TOP of CHANGELOG.md (above `## 2.0.28-14`), matching the existing loose bullet style. Content (4-6 bullets):

```
## 5.0.2-1

- Replaced retry engine: removed `com.github.rholder:guava-retrying:2.0.0`, added `dev.failsafe:failsafe:3.3.2`.
- **Breaking change:** Exhaustion exception type changed from guava's checked `RetryException` to failsafe's unchecked `FailsafeException` (or the raw original throwable). Consumers catching `RetryException` by type will get a compile error — see [MIGRATION.md](MIGRATION.md).
- `RetryStrategy.execute()` signature and `throws Exception` declaration are unchanged. Synchronous blocking behavior is preserved.
- All retry semantics (attempt counts, wait sequences, exception filtering) verified behaviorally identical via 15 parity tests on the failsafe-only tree (49 total tests green).
- See [MIGRATION.md](MIGRATION.md) for migration instructions and version references.
```

**README.md** (optional, per D-06 discretion):

Add a single line under the existing content (after the NOTE section) pointing to MIGRATION.md:

```
## Migration
See [MIGRATION.md](MIGRATION.md) for retry-engine migration notes (guava-retrying → failsafe).
```

  </action>
  <verify>
    <automated>grep -q "5.0.2-1" CHANGELOG.md &amp;&amp; grep -q "FailsafeException" CHANGELOG.md &amp;&amp; grep -q "MIGRATION.md" CHANGELOG.md &amp;&amp; grep -q "MIGRATION.md" README.md &amp;&amp; echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>CHANGELOG.md has a 5.0.2-1 section at top naming the exception-type change, flagging it as breaking, linking to MIGRATION.md. README.md has a one-line pointer to MIGRATION.md.</done>
</task>

</tasks>

<verification>
1. `MIGRATION.md` exists at repo root and contains: "throws Exception", "3.3.2", "2.0.0", "FailsafeException", "RetryException"
2. `CHANGELOG.md` has a `## 5.0.2-1` section at the top (above `## 2.0.28-14`) containing "FailsafeException" and a link to MIGRATION.md
3. `README.md` contains a link to MIGRATION.md
4. No source files under `src/` were modified (docs-only phase)
5. Content is consistent with 03-VERIFICATION.md: 49 tests green, guava-retrying removed, failsafe 3.3.2 present, exception type is FailsafeException/raw throwable
</verification>

<success_criteria>

1. Changelog entry names the RetryException → FailsafeException/raw-throwable change and flags it as a transitive compile break for consumers catching RetryException by type. (DOC-01)
2. Migration note states that RetryStrategy.execute() retains its throws Exception declaration and that synchronous blocking behavior is unchanged. (DOC-02)
3. Both docs reference the verified failsafe version (3.3.2) and the removed guava-retrying coordinate (com.github.rholder:guava-retrying:2.0.0).
4. Changelog links to MIGRATION.md.
5. No source code changes — documentation only.
</success_criteria>

<output>
After completion, create `.planning/phases/04-release-documentation/04-01-SUMMARY.md`
</output>
