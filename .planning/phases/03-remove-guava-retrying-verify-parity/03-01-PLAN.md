---
phase: 03-remove-guava-retrying-verify-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
autonomous: true
requirements:
  - DEP-02
  - DEP-03
  - ENG-05
  - VER-01
must_haves:
  truths:
    - "pom.xml contains no com.github.rholder dependency block"
    - "pom.xml contains no guava-retrying.version property"
    - "mvn test passes on the failsafe-only tree"
    - "No source file references com.github.rholder"
  artifacts:
    - path: "pom.xml"
      provides: "Failsafe-only dependency tree — guava-retrying removed"
      contains: "dev.failsafe"
  key_links:
    - from: "pom.xml"
      to: "src/main/java/.../retry/RetryStrategy.java"
      via: "failsafe dependency resolves on classpath"
      pattern: "dev\\.failsafe"
---

<objective>
Remove guava-retrying from pom.xml (dependency block + version property) and verify the failsafe-only tree compiles and all existing tests pass.

Purpose: Parity tests in Plan 02 must run against a tree with guava-retrying gone — running them with both libs present can mask drift. This plan creates the clean tree.

Output: pom.xml with guava-retrying fully removed; `mvn test` green on the failsafe-only tree.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-remove-guava-retrying-verify-parity/03-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove guava-retrying dependency and version property from pom.xml</name>
  <files>pom.xml</files>
  <read_first>
    - pom.xml (lines 100-190 — see the guava-retrying.version property at line 107 and the dependency block at lines 179-183)
    - src/main/java/io/appform/dropwizard/actors/retry/RetryStrategy.java (confirm source already uses failsafe, not guava-retrying)
  </read_first>
  <action>
Remove two items from pom.xml:

1. Delete the version property at line 107:
   `<guava-retrying.version>2.0.0</guava-retrying.version>`

2. Delete the 5-line dependency block at lines 179-183:

   ```xml
   <dependency>
       <groupId>com.github.rholder</groupId>
       <artifactId>guava-retrying</artifactId>
       <version>${guava-retrying.version}</version>
   </dependency>
   ```

Do NOT remove the `dev.failsafe:failsafe` dependency block (lines 184-188) — that stays.
Do NOT remove the `com.google.guava:guava` dependency management entry (lines 128-132) — guava remains available via dropwizard-core (provided scope).

After removal, verify with:

```bash
grep -n "guava-retrying\|com.github.rholder" pom.xml
```

This must return zero matches.
  </action>
  <verify>
    <automated>! grep -q "guava-retrying\|com.github.rholder" pom.xml && echo "PASS: no guava-retrying refs" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "guava-retrying" pom.xml` returns 0
    - `grep -c "com.github.rholder" pom.xml` returns 0
    - `grep -c "dev.failsafe" pom.xml` returns at least 2 (property + dependency)
    - `grep -c "failsafe.version" pom.xml` returns 1 (property still present)
  </acceptance_criteria>
  <done>pom.xml has zero references to guava-retrying or com.github.rholder; failsafe dependency remains.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify failsafe-only tree compiles and existing tests pass</name>
  <files>pom.xml</files>
  <read_first>
    - pom.xml (confirm guava-retrying removed in Task 1)
  </read_first>
  <action>
Run the full build and test suite on the failsafe-only tree:

```bash
mvn clean test
```

This verifies:

- DEP-02/DEP-03: guava-retrying fully removed, tree still compiles
- ENG-05: no source references to com.github.rholder (compilation would fail if any import remained)
- VER-01: existing tests pass against the failsafe-only tree

If any test fails due to a missing guava-retrying class, that means a source file still imports com.github.rholder — but Phase 2 already verified zero source references. The build should pass.

If the build fails with a guava class not found (e.g., `com.google.common.base.Strings`), that is NOT a guava-retrying issue — guava remains available via dropwizard-core (provided scope). Check that the guava dependency management entry is still in pom.xml.
  </action>
  <verify>
    <automated>mvn clean test -q 2>&1 | tail -5 && grep -q "BUILD SUCCESS" <(mvn test -q 2>&1 | tail -20) && echo "PASS" || echo "CHECK OUTPUT"</automated>
  </verify>
  <acceptance_criteria>
    - `mvn clean test` exits 0
    - `mvn clean test` output contains "BUILD SUCCESS"
    - `grep -rn "com.github.rholder" src/ pom.xml` (excluding target/) returns 0 matches
  </acceptance_criteria>
  <done>mvn clean test passes on the failsafe-only tree with zero references to com.github.rholder in source or pom.</done>
</task>

</tasks>

<verification>
- `grep -rn "com.github.rholder" pom.xml src/` (excluding target/) returns zero matches
- `grep -c "guava-retrying" pom.xml` returns 0
- `mvn clean test` passes (BUILD SUCCESS)
</verification>

<success_criteria>

- guava-retrying dependency block removed from pom.xml (DEP-02)
- guava-retrying.version property removed from pom.xml (DEP-03)
- No source or pom references to com.github.rholder remain (ENG-05)
- All existing tests pass on the failsafe-only tree (VER-01)
</success_criteria>

<output>
After completion, create `.planning/phases/03-remove-guava-retrying-verify-parity/03-01-SUMMARY.md`
</output>
